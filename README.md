NyanRush
========

##Installation
`git clone https://github.com/ciembor/NyanRush.git`

`cd NyanRush`

`bundle install`

##Execution
`ruby nyan_rush.rb`

##Controls
`w` or `k` - move up

`s` or `j` - move down

`q` - exit game